import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.99lineofcode',
  appName: 'rafayblog',
  webDir: '.vercel/output/static/'
};

export default config;
